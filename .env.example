# Copy to .env.local (gitignored) to enable gameplay telemetry in dev.
# Empty / unset → telemetry is fully disabled (no network calls).
# Points the game at the local telemetry backend (sim/server/, default port 8787).
VITE_TELEMETRY_URL=http://127.0.0.1:8787
