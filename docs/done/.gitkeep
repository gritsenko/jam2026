# Выполненные (архив реализованных спек). Пока пусто.
# Сюда переезжает спека из ../planned/, когда фича полностью реализована —
# заморозь её здесь и отрази поведение в ../working/current-state.md.
# См. ../README.md, раздел «Правило синхронизации».
