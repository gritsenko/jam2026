# --- secrets ---
tools/.gemini_key
.gemini_key
.env
.env.local
.env.*.local
*.key

# --- node ---
node_modules/
dist/

# --- sim/telemetry artifacts (DB, runs, generated dashboard) ---
sim/out/

# --- generated / intermediate sprites ---
# Raw 1K-2K generations before post-processing. Final sprites in
# assets/sprites/ ARE committed; raw ones are disposable.
assets/raw/

# --- python ---
tools/.venv/
__pycache__/
*.pyc
.DS_Store

# Local Cursor skills (per-machine copy of gamejam-spark)
gamejam-spark/

# Local Claude Code settings (per-machine)
.claude/settings.local.json

# MCP server config — machine-specific paths (uvx) + references a local API key
.mcp.json
