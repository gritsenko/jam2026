# Production build telemetry endpoint. Auto-loaded by `vite build` (production mode),
# so GitHub Pages deploys (and local `npm run build`/`preview`) point the game at the
# live analytics backend. Empty / unset → telemetry disabled.
# NOTE: temporary analytics server (may be torn down) — see sim/server/README.md.
VITE_TELEMETRY_URL=https://llm.gritsenko.biz
